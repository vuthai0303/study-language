# AI Work Log

## Objectives
Thêm chức năng Học nghe cho StudyLanguage với 3 trình độ (Cơ bản, Trung cấp, Chuyên nghiệp), có AI sinh bài, phát âm thanh bằng TTS, chấm đúng/sai và hiển thị transcript sau khi nộp bài. Đồng thời khôi phục `guide.md` về nội dung gốc và chỉ bổ sung phần Listening.

## Plan
1. Tạo route mới `/listening` và các component con cho selector / practice.
2. Bổ sung type, localStorage key, prompt AI và lịch sử bài nghe.
3. Thêm điều hướng, card trang chủ, và chỉ thêm phần Listening vào `guide.md`.
4. Chạy lint/build và tạo work log.

## Changed
- `src/types/listening.ts`: thêm type cho level, segment, question và practice của bài nghe.
- `src/consts/index.ts`: thêm `DEFAULT_LISTENING_TOPIC` và key `LISTENING_HISTORY_PARAGRAPH`.
- `src/lib/localStorage.ts`: thêm helper đọc/ghi lịch sử bài nghe.
- `src/components/listening/listening-selector.tsx`: tạo UI chọn trình độ/chủ đề và nút tạo/hủy.
- `src/components/listening/listening-practice.tsx`: render bài nghe, phát audio, chọn đáp án, chấm điểm và hiển thị transcript.
- `src/app/listening/page.tsx`: điều phối flow tạo bài, parse JSON AI, lưu lịch sử và render practice.
- `src/components/navigation.tsx`: thêm link tới `/listening`.
- `src/app/page.tsx`: thêm card trang chủ cho Học nghe.
- `guide.md`: khôi phục nội dung gốc từ Git và chỉ nối thêm mục Listening ở cuối.

## Result
- Tính năng Học nghe đã được thêm với 3 trình độ, TTS, loading, lỗi và kết quả đúng/sai.
- `guide.md` đã được trả về nội dung gốc, chỉ bổ sung thêm phần Listening ở cuối.
- Validation:
  - `npm run lint`: passed.