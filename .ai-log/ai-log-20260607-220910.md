# AI Work Log

## Objectives

Bổ sung thêm chức năng phát âm (voice) để đọc câu tiếng Anh trong bài học chế độ "Câu văn" (Sentence), ở chiều luyện tập Việt → Anh.

## Plan

- Mở file `sentence.tsx`.
- Thêm icon `Speech` và button gọi hàm `speak(englishTarget)` hiển thị bên cạnh câu tiếng Việt của chế độ việt → anh.
- Kiểm tra tính hợp lệ qua `npm run lint` và `npm run build`.

## Changed

- `src/components/vocabulary/vocabulary-study/sentence.tsx`:
  - Trong phần giao diện của chế độ Việt → Anh (`vi_to_en`), bổ sung thêm `Button` phát âm nằm cùng dòng với hiển thị `vietnameseSentence`. Khi nhấn vào sẽ kích hoạt đọc `englishTarget`.

## Result

- Thêm chức năng phát âm thành công cho chế độ "Việt → Anh".
- Đã chạy `npm run lint` và `npm run build` không có lỗi.
